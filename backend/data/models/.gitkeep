Trained sklearn models (e.g. core_perm_model.joblib) are saved here.
Contents are gitignored (regenerable via scripts/train_core_perm_model.py) --
this file just keeps the empty folder tracked in git.

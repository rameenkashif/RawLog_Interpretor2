# Copy to `.env` for local overrides. Not required for local dev -- Vite's
# dev server proxies API calls to http://127.0.0.1:8000 by default (see
# vite.config.ts). Set this only when pointing at a deployed backend.
VITE_API_BASE_URL=
